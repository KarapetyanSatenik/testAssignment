# If provided, the app will use OpenAI for LLM responses; otherwise it uses a mock LLM.
OPENAI_API_KEY=
# OpenAI model (optional, defaults to gpt-4o-mini if key is present)
OPENAI_MODEL=gpt-4o-mini
# Server port
PORT=3000
